# Decision-Tree-
A machine learning algorithm for classification &amp; regression. It splits data based on feature questions, forming a flowchart of nodes and leaves to make predictions. Easy to interpret, handles numeric &amp; categorical data, but can overfit if not pruned.
